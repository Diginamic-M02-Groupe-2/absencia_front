import { AbsenceTypePipe } from './absence-type.pipe';

describe('AbsenceTypePipePipe', () => {
  it('create an instance', () => {
    const pipe = new AbsenceTypePipe();
    expect(pipe).toBeTruthy();
  });
});
